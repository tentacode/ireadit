{% extends 'base_with_sidebar.html.twig' %}

{% block title %}
  Mes liens
{% endblock %}

{% block content %}

  {% include 'links/add_link_modal.html.twig' %}
  
  <!-- Section 1 -->
  <section class="relative w-full bg-white">
    <div class="absolute w-full h-32 bg-gradient-to-b from-purple-200 to-white"></div>
    <div class="relative w-full px-5 py-0 mx-auto sm:py-10 md:py-10 md:px-10 max-w-7xl">
      {# <p class="text-lg font-medium text-gray-500 sm:text-2xl">
        Designs and layouts to help you with your app.
      </p> #}

        <div class="columns-3">

          {% for link in links %}
            <div class="mb-5 flex flex-col items-start col-span-12 overflow-hidden rounded-xl md:col-span-6 lg:col-span-4 border border-gray-200">
              {% if link.imageUrl %}
                <a href="{{ link.url }}"
                  class="block w-full transition duration-200 ease-out transform hover:scale-110">
                  <img class="object-cover w-full max-h-56"
                    src="{{ link.imageUrl }}" />
                </a>
              {% else %}
                <div class="w-full h-16 bg-gradient-to-b from-purple-100 to-purple-300"></div>
              {% endif %}
              
              <div class="w-full relative flex flex-col items-start px-6 bg-white py-7 border-0 border-t-2 border-gray-200">
                <div class="absolute top-0 -mt-4 flex items-center  leading-none w-auto inline-block  inline-block">
                  <span class=" ml-1 text-xl p-0 font-medium uppercase text-green-400 bg-white rounded-full">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 " viewBox="0 0 20 20" fill="currentColor">
                      <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                    </svg>
                  </span>

                  <div class="ml-14 flex -space-x-3">
                    <span class="inline-block text-center text-xl pt-0.5 h-8 w-8 rounded-full bg-white ring-1 ring-slate-200">👍</span>
                    <span class="inline-block text-center text-xl pt-0.5 h-8 w-8 rounded-full bg-white ring-1 ring-slate-200">😂</span>
                    <span class="inline-block text-center text-xl pt-0.5 h-8 w-8 rounded-full bg-white ring-1 ring-slate-200">💖</span>
                    <span class="inline-block text-center text-xl pt-0.5 h-8 w-8 rounded-full bg-white ring-1 ring-slate-200">🤡</span>
                  </div>
                </div>
                <h2 class="text-base font-bold sm:text-lg md:text-xl">
                  <a href="{{ link.url }}">{{ link.title }}</a>
                </h2>
                <p class="mt-2 text-sm text-gray-500">
                  {{ link.description }}
                </p>
                {# <span class="mt-2 bg-slate-200 px-2 py-1 rounded-full text-[0.6rem] font-medium uppercase text-black">{{ link.type }}</span> #}
              </div>
            </div>
          {% endfor %}


        </div>
    </div>
  </section>
{% endblock %}
